# Applied after shield defaults, only for the NOLOG build targets.
CONFIG_ZMK_USB_LOGGING=n
CONFIG_LOG=n

# Disable Studio and the custom RPC integrations enabled by the base shields.
CONFIG_ZMK_STUDIO=n
CONFIG_ZMK_RUNTIME_INPUT_PROCESSOR_STUDIO_RPC=n
CONFIG_ZMK_BLE_MANAGEMENT_STUDIO_RPC=n
CONFIG_ZMK_SETTINGS_RPC_STUDIO=n
